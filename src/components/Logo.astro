---
import { Image } from 'astro:assets';
import logo from '~/assets/images/logo-1.png'; // Adjust the import path to where your logo image is saved
import { SITE } from '~/utils/config';
---

<Image 
  src={logo} 
  alt={`${SITE?.name} Logo`}
 class="self-center ml-2 rtl:ml-0 rtl:mr-2 h-12 w-auto md:h-15 dark:filter dark:brightness-90"
/>
